import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("PathWise")
            .font(AppTypography.largeTitle)
            .foregroundStyle(AppColors.primaryPurple)
    }
}
